/**
 ** @author {your name}
 ** @function
 ** @return {array} or {undefined}
 * @param collection
 * @param predicate
 **/
export default function find(collection, predicate) {
  // Given a collection of elements and a predicate, find will return the first element that
  // matches the predicate.
}
