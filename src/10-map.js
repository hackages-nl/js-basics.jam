/**
 ** @author {your name}
 ** @function
 ** @return {array}
 * @param collection
 * @param callback
 **/
export default function map(collection, callback) {
  // Given a collection of elements and a callback, the map function will call the callback function
  // by passing as arguments to the callback each item of the collection, its position with the collection and
  // the collection itself. This map returns a collection containing the newly calculated items
}
