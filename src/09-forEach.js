/**
 ** @author {your name}
 ** @function
 ** @return
 * @param collection
 * @param callback
 **/
export default function forEach(collection, callback) {
  // Given a collection of elements and a callback, the forEach function will call the callback function
  // by passing as arguments to the callback each item of the collection, its position with the collection and
  // the collection itself. This forEach function doesn't return anything
}
